{
  "type": "module",
  "dependencies": {
    "dotenv": "^16.5.0",       // Gerenciamento de variáveis de ambiente
    "express": "^5.1.0",       // Framework web
    "firebase-admin": "^13.2.0", // SDK Admin do Firebase para o servidor
    "node-cron": "^3.0.3"      // Agendamento de tarefas
  },
  "scripts": {
    "start": "node server.js",  // Inicia servidor principal
    "reset": "node reset.js"    // Executa script de reset do Firebase
  }
}